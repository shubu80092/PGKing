<!-- Sidebar -->
<nav class="admin-sidebar d-flex flex-column flex-shrink-0 shadow-lg" style="width: 280px; background: #0F172A; min-height: 100vh;">
    <div class="px-4 py-4 text-center">
        <a href="/" class="text-decoration-none">
            <img src="/images/logo.png" alt="PGKing Logo" height="80" class="mb-2">
            <h5 class="text-white fw-bold mb-0" style="letter-spacing: 2px;">ADMIN PANEL</h5>
        </a>
    </div>
    
    <div class="px-3 mb-4">
        <div class="bg-primary bg-opacity-10 rounded-4 p-3 d-flex align-items-center">
            <img src="https://ui-avatars.com/api/?name=Admin+User&background=0052FF&color=fff" width="40" height="40" class="rounded-circle me-3 border border-2 border-white shadow-sm">
            <div>
                <p class="text-white small fw-bold mb-0">Admin User</p>
                <span class="badge bg-success" style="font-size: 0.6rem;">Super Admin</span>
            </div>
        </div>
    </div>

    <ul class="nav nav-pills flex-column mb-auto px-3 gap-2">
        <li class="nav-item">
            <a asp-controller="Admin" asp-action="Dashboard" class="nav-link d-flex align-items-center py-3 rounded-4 @(ViewContext.RouteData.Values["Action"]?.ToString() == "Dashboard" ? "active shadow-primary" : "text-gray-400")">
                <i class="fas fa-grid-2 fa-fw me-3 fs-5"></i>
                <span class="fw-semibold">Dashboard</span>
            </a>
        </li>
        <li>
            <a asp-controller="Admin" asp-action="Properties" class="nav-link d-flex align-items-center py-3 rounded-4 @(ViewContext.RouteData.Values["Action"]?.ToString() == "Properties" ? "active shadow-primary" : "text-gray-400")">
                <i class="fas fa-building fa-fw me-3 fs-5"></i>
                <span class="fw-semibold">Properties</span>
            </a>
        </li>
        <li>
            <a asp-controller="Admin" asp-action="Banners" class="nav-link d-flex align-items-center py-3 rounded-4 @(ViewContext.RouteData.Values["Action"]?.ToString() == "Banners" ? "active shadow-primary" : "text-gray-400")">
                <i class="fas fa-images fa-fw me-3 fs-5"></i>
                <span class="fw-semibold">Banners</span>
            </a>
        </li>
        <li>
            <a asp-controller="Admin" asp-action="Clients" class="nav-link d-flex align-items-center py-3 rounded-4 @(ViewContext.RouteData.Values["Action"]?.ToString() == "Clients" ? "active shadow-primary" : "text-gray-400")">
                <i class="fas fa-users fa-fw me-3 fs-5"></i>
                <span class="fw-semibold">Clients / Tenants</span>
            </a>
        </li>
        <li>
            <a asp-controller="Admin" asp-action="Bookings" class="nav-link d-flex align-items-center py-3 rounded-4 @(ViewContext.RouteData.Values["Action"]?.ToString() == "Bookings" ? "active shadow-primary" : "text-gray-400")">
                <i class="fas fa-calendar-check fa-fw me-3 fs-5"></i>
                <span class="fw-semibold">Bookings</span>
            </a>
        </li>
    </ul>

    <div class="px-3 pb-4 mt-auto">
        <a asp-controller="Auth" asp-action="Logout" class="btn btn-outline-danger w-100 rounded-pill fw-bold py-2">
            <i class="fas fa-sign-out-alt me-2"></i> Logout
        </a>
    </div>
</nav>

<style>
    .text-gray-400 { color: #94A3B8 !important; }
    .nav-link:hover { background: rgba(255,255,255,0.05); color: white !important; }
    .nav-link.active { background: #0052FF !important; color: white !important; }
    .shadow-primary { box-shadow: 0 10px 15px -3px rgba(0, 82, 255, 0.4); }
</style>
